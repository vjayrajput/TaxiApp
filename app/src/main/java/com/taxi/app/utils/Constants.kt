package com.taxi.app.utils

import com.taxi.app.BuildConfig

const val CONNECTION_TIMEOUT = 60 //mins

const val FILE_PATH_HTTP = "http"

const val ACTION_LOGOUT = BuildConfig.APPLICATION_ID + ".action.ACTION_LOGOUT"

